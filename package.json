{
  "name": "agricola",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w @agricola/server\" \"npm run dev -w @agricola/client\"",
    "build": "npm run build -w @agricola/client && npm run build -w @agricola/server",
    "start": "npm run start -w @agricola/server",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
